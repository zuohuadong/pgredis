# Benchmark

No benchmark has been run in this repository yet.

Run the manual `Benchmark` GitHub Actions workflow or run locally with:

```bash
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/pgredis \
REDIS_URL=redis://127.0.0.1:6379 \
bun run benchmark
```
