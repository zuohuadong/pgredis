{
  "name": "pgredis-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/bun-listen && bun run build && cd ../pgredis && bun run build",
    "check": "cd packages/bun-listen && bun run check && cd ../pgredis && bun run check && cd ../.. && bunx tsc --noEmit -p tsconfig.json",
    "test": "bun test packages/",
    "test:integration": "bun test packages/pgredis/src/integration.test.ts",
    "smoke:pack": "node scripts/smoke-packages.mjs",
    "benchmark": "bun run build && bun run benchmark/bench.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.0.0",
    "@types/pg": "^8.16.0",
    "ioredis": "^5.8.2",
    "pg": "^8.16.3",
    "tsup": "^8.5.0",
    "typescript": "^6.0.3"
  }
}
